/**
 * State store responsible for interacting with API.
 *
 * Does not really have any state except for keeping track
 * of tokens and keeping track of network status.
 */
class ApiStore {}

export default ApiStore;
